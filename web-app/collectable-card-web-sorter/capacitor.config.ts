import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'collectable-card-web-sorter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
